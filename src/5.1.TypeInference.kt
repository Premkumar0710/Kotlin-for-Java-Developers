fun main() {

   /* Here we need not use syntax like the previous file. We can use a feature called Type Inference.
      So, we need not give any DataType here
   */
    
    var num = 10
    var bigNum = 100L
    var decimalNum = 0.01
    var bigDecimalNum  = 3.14F
    var boolean = false
    var s  = "Hello all !!"
    var c = 'A'
}